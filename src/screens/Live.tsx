import React from 'react';
import {View, Text} from 'react-native';

import {LiveScreenProps} from '../types/screen';

const Live = ({navigation}: LiveScreenProps) => {
  return (
    <View>
      <Text>Live page</Text>
    </View>
  );
};

export default Live;
