import React from 'react';
import {View, Text} from 'react-native';

import {MallScreenProps} from '../types/screen';

const Mall = () => {
  return (
    <View>
      <Text>Mail page</Text>
    </View>
  );
};

export default Mall;
